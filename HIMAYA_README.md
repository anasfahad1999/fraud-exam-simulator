# حزمة حماية "ثغرة" — كل الإجراءات المجانية الممكنة

هذي حزمة جاهزة تطبّقها على مشروعك الحالي **بدون باك-إند وبدون أي تكلفة**.
مرتّبة بالأولوية: من الأعلى أثراً للأقل.

> **الصدق أولاً:** تطبيقك يشتغل كامل في المتصفح، فأي محتوى يوصل للطالب = ممكن
> سحبه نظرياً. الإجراءات التالية **ترفع تكلفة السرقة وتوقف 90–95% من الحالات
> العابرة والبوتات البسيطة**، لكنها لا تمنع شخصاً خبيراً ومصمّماً ومعه وقت.
> الحماية الكاملة تحتاج نقل الأسئلة لسيرفر (وهذا تأجّل للميزانية لاحقاً).

---

## الأولوية ١ — Cloudflare (المجاني) أمام موقعك ⭐ أكبر مكسب مجاني

هذا **أهم إجراء** وما يحتاج كود، بس حساب مجاني. يقعد كطبقة قدام موقعك:

1. سجّل في Cloudflare (الخطة Free) واربط نطاقك (Domain).
2. فعّل من لوحة التحكم:
   - **Bot Fight Mode** (Security → Bots): يصد أغلب بوتات السحب الآلية.
   - **Scrape Shield** (Scrape Shield): حماية من كشط المحتوى + إخفاء الإيميلات.
   - **Hotlink Protection**: يمنع المواقع الثانية من سرقة صورك/اللوقو.
   - **SSL/TLS → Full (strict)** + **Always Use HTTPS**.
3. (اختياري قوي ومجاني) **Cloudflare Turnstile**: بوابة تحقّق "أنت إنسان" تحطها
   قبل دخول قسم الأسئلة. توقف السحب الآلي بشكل كبير.
4. (لو متوفر بخطتك) قاعدة **Rate Limiting** بسيطة: مثلاً حظر أي IP يطلب صفحات
   كثيرة في وقت قصير.

> ليه هذا الأهم؟ لأن أكبر خطر فعلي عليك هو **السحب الآلي** لكامل البنك، و
> Cloudflare يكسر هذا تحديداً، مجاناً، بدون ما تعدّل سطر كود.

---

## الأولوية ٢ — تقوية البناء (تمنع قراءة الكود والبيانات)

ملفات جاهزة في الحزمة:

### أ) `vite.config.ts` (استبدل ملفك الحالي)
يسوّي ثلاثة أشياء:
- **يلغي خرائط المصدر** (`sourcemap: false`) — بدونها يستحيل ترجيع كودك الأصلي
  المرتّب. (هذا لوحده مهم جداً.)
- **تعمية (obfuscation)** لكل الكود — يحوّل النصوص العربية لمصفوفة مشفّرة base64،
  فما يقدر أحد يفتح ملف الـbundle و"يكتل-سي / يكتل-في" أسئلتك كنص واضح.
- **يحذف console** ويصغّر الكود (terser).

### ب) تثبيت المكتبة المطلوبة للتعمية
```bash
npm install -D vite-plugin-javascript-obfuscator terser
```

### ج) جرّب البناء محلياً
```bash
npm run build
npm run preview
```
**مهم:** افتح الموقع بعد البناء وتأكد كل شي يشتغل، لأن ملف بياناتك كبير (1.5 ميجا)
والتعمية تزيد الحجم وتبطّئ التحميل شوي. لو حسّيت ثقل:
- خفّف `stringArrayThreshold` من `0.7` إلى `0.5` أو `0.4`.
- أو اجعل `stringArray: false` (تبقى التعمية للكود بدون تثقيل النصوص).
- لو طلع تعارض مع terser، خلّ `minify: false` واترك compact للتعمية وحدها.

---

## الأولوية ٣ — رؤوس الأمان (Security Headers) مجانية

ملفات جاهزة حسب مستضيفك:
- **Cloudflare Pages / Netlify** → استخدم `public/_headers`.
- **Vercel** → استخدم `vercel.json`.

تعطيك: منع تضمين موقعك داخل إطار (clickjacking)، فرض HTTPS، وسياسة محتوى (CSP)
تقلّل مخاطر حقن السكربتات.

> **انتبه على CSP:** لو الموقع طلع أبيض بعد التطبيق، غالباً CSP منعت شي. افتح
> Console وشوف الخطأ، وعدّل السطر. أكثر سبب شائع: خط أو سكربت من نطاق خارجي ما هو
> مذكور — أضِف نطاقه للسطر المناسب. جرّبها على نسخة تجريبية أول.

---

## الأولوية ٤ — قفل الحسابات (مجاني، خطوة دقايق)

أكثر "تهكير" حقيقي يصير بسبب حساب مخترق مو ثغرة برمجية:
- فعّل **التحقق بخطوتين (2FA)** على: حساب الاستضافة، GitHub/مستودع الكود،
  حساب Cloudflare، والإيميل المرتبط فيهم.
- لا تحط أي مفتاح/كلمة سر داخل الكود (نسختك الحالية نظيفة — حافظ على هذا).
- حدّث المكتبات وراقب الثغرات: `npm audit`.

---

## الأولوية ٥ — احتكاك المتصفح (يوقف الناسخ العابر فقط)

ملفات جاهزة: `src/security.ts` + `src/main.tsx` (استبدل ملفك).
يعطّل الزر الأيمن، اختصارات أدوات المطوّر (F12...)، وسحب الصور.

> صريح معك: هذا **أضعف إجراء** وسهل تجاوزه، وبعضه يضايق الطالب العادي. عشان كذا
> خليت "تعطيل تحديد النص" و"منع النسخ" **مطفيّين افتراضياً** داخل الملف — شغّلهم
> فقط إذا تبي تشدّد وتتحمّل إزعاج بسيط للمستخدم. لا تعتمد على هالطبقة لحالها.

---

## ما لا يستحق وقتك (حماية شكلية)
- "كشف فتح أدوات المطوّر" وإعادة التوجيه: إيجابيات كاذبة كثيرة ويتجاوزونه بسهولة.
- تشفير البيانات في الواجهة بمفتاح موجود بنفس الواجهة: مو تشفير حقيقي، أي أحد
  يقرأ المفتاح من نفس الكود. (التعمية اللي طبّقناها أصدق وأخف.)

---

## خلاصة الترتيب لو عندك وقت محدود
1. Cloudflare Free + Turnstile  ← افعلها اليوم، أكبر أثر.
2. استبدل `vite.config.ts` + ثبّت المكتبة + ابنِ وجرّب.
3. ضِف ملف رؤوس الأمان حسب مستضيفك.
4. فعّل 2FA على كل الحسابات.
5. ضِف احتكاك المتصفح (اختياري).

ولمّا تجي الميزانية: انقل بنك الأسئلة لسيرفر مع **تصحيح الإجابات في الخادم +
بصمة لكل حساب** — وقتها تنتقل من "صعّبنا السرقة" إلى "حميناها فعلاً".
