# معاينة لوحة المطور محليًا

هذه المعاينة مخصصة فقط لتقييم شكل لوحة المطور قبل ربط نظام الدخول الحقيقي.

## التشغيل

1. انسخ ملف `.env.local.example` باسم `.env.local` أو أنشئ ملف `.env.local` في جذر المشروع.
2. ضع داخله:

```txt
VITE_ENABLE_DEV_PREVIEW=true
```

3. شغل الموقع:

```bash
npm run dev
```

4. افتح:

```txt
http://localhost:5173/developer
```

إذا كان المنفذ مختلفًا كما يظهر عندك، استخدم نفس المنفذ، مثل:

```txt
http://localhost:5180/developer
```

## ملاحظة أمان

هذه المعاينة لا تعمل إلا في وضع التطوير المحلي `import.meta.env.DEV` وعلى `localhost` أو `127.0.0.1`.
في النسخة المنشورة لا تفتح لوحة المطور إلا عبر نظام المصادقة والصلاحيات المرتبط من جهة الخادم.
